{
  "name": "mfe-example",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "kill-ports": "npx kill-port --port 3000,3001,3002,3003,3004,5001",
    "clean": "lerna clean -y && lerna run clean && rm -rf build node_modules",
    "clean:build": "lerna run clean --stream",
    "develop": "npm run kill-ports && BROWSER=none NODE_ENV=development lerna run develop --stream",
    "start": "npm run kill-ports && lerna run start --stream",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream"
  },
  "author": "Thiago Mendonca <thiago.mendonca@gft.com>",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
